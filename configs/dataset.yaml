dataset_id: "yunfan-y/trump-qa"
dataset_origin: "huggingface"
split: "train"
# Ajusta estos campos tras inspeccionar dataset.features en la EDA
columns:
  instruction_field: "instruction"    # p.ej: "You are impersonating Donald Trump. Answer the question."
  question_field: "question"          # pregunta a responder
  context_field: "input"              # texto base (tweet/post/mensaje) si aplica
  target_field: null                  # si no hay ground-truth, se deja null para SFT sin etiquetas
val_size: 0.2
max_samples: null
